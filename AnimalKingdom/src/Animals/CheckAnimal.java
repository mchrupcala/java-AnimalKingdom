package Animals;

public interface CheckAnimal
{
	//variable name: test...takes in the parent class AbstractVehicle...I didn't know you could do that? Well...the variable 'vehicles' in Main gives me the ArrayList...maybe that's where they're all passed in. So why AbstractVehicle then?
	boolean test(AbstractAnimals a);
}